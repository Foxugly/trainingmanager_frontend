import { TranslocoGlobalConfig } from '@jsverse/transloco-utils';

const config: TranslocoGlobalConfig = {
  rootTranslationsPath: 'public/i18n/',
  langs: ['fr', 'nl', 'en', 'it', 'es'],
  keysManager: {},
};

export default config;
